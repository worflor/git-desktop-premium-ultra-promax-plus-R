# Manifold Git Client

Your Personal Git Client.

## Branding

Runtime app identity is centralized in `lib/app/app_identity.dart`.

Update `shortName`, `fullName`, `description`, and `tag` there to rename the
app or change the branch/build badge.
