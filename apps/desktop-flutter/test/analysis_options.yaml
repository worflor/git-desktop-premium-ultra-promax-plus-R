# Tests inherit the full root config (strict modes + the error-tier lints) and
# relax only what is genuinely correct in test code:
#   • avoid_print — tests and profilers print their results as output.
include: ../analysis_options.yaml

linter:
  rules:
    avoid_print: false
